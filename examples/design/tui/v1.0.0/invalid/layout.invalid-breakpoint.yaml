# Invalid: Breakpoint tier not in allowed enum
$schema: https://schemas.fulmenhq.dev/crucible/design/tui/v1.0.0/layout.schema.json
version: "1.0.0"
breakpoints:
  - name: "default"
    min_cols: 80
    tier: "extra-large" # Not a valid tier - should fail
spacing:
  md:
    cols: 2
    rows: 1
