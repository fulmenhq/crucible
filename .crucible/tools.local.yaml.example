# Local tool overrides for development iteration
# Copy this file to tools.local.yaml (gitignored) and customize paths.
# Bootstrap prefers tools.local.yaml when present, allowing you to test local builds.
version: v1.0.0
binDir: ./bin
tools:
  - id: fuldx
    description: Fulmen Developer Toolkit (local build)
    required: false
    install:
      type: link
      source: /Users/yourname/dev/fulmenhq/fuldx/dist/fuldx
      binName: fuldx
      destination: ./bin
