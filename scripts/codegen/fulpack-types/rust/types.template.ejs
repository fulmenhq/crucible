//! Fulpack types - generated from Crucible SSOT
//!
//! Version: <%- version %>
//! Source: schemas/library/fulpack/v1.0.0/

use serde::{Deserialize, Serialize};

// ============================================================================
// Enums
// ============================================================================

<% taxonomies.forEach(taxonomy => { %>
/// <%- taxonomy.className %> represents <%- taxonomy.description.toLowerCase() %>.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum <%- taxonomy.className %> {
<% taxonomy.items.forEach(item => { %>    /// <%- item.description %>
    #[serde(rename = "<%- item.id %>")]
    <%- item.variant_name %>,
<% }); %>}

impl std::fmt::Display for <%- taxonomy.className %> {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
<% taxonomy.items.forEach(item => { %>            Self::<%- item.variant_name %> => write!(f, "<%- item.id %>"),
<% }); %>        }
    }
}
<% }); %>

// ============================================================================
// Data Structures
// ============================================================================

<% data_structures.forEach(schema => { %>
/// <%- schema.className %> <%- schema.description.toLowerCase() %>.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct <%- schema.className %> {
<% schema.required_properties.forEach(prop => { %>    /// <%- prop.description %>
    #[serde(rename = "<%- prop.name %>")]
    pub <%- prop.name_rust %>: <%- prop.rust_type %>,
<% }); %>
<% schema.optional_properties.forEach(prop => { %>    /// <%- prop.description %>
    #[serde(rename = "<%- prop.name %>")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub <%- prop.name_rust %>: <%- prop.rust_type %>,
<% }); %>}
<% }); %>

// ============================================================================
// Options
// ============================================================================

<% options.forEach(schema => { %>
/// <%- schema.className %> <%- schema.description.toLowerCase() %>.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, Default)]
#[serde(rename_all = "camelCase")]
pub struct <%- schema.className %> {
<% schema.properties.forEach(prop => { %>    /// <%- prop.description %>
    #[serde(rename = "<%- prop.name %>")]
    #[serde(skip_serializing_if = "Option::is_none")]
    pub <%- prop.name_rust %>: <%- prop.rust_type %>,
<% }); %>}
<% }); %>

/// Fulpack module version
pub const FULPACK_VERSION: &str = "<%- version %>";
