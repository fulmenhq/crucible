package fulhash

// Code generated by scripts/codegen/generate-fulhash-types.ts; DO NOT EDIT.

// Algorithm identifier
type Algorithm string

const (
<% taxonomies[0].items.forEach(function(item) { %>
	// <%= item.display_name %>
	// <%= item.description %>
	<%= item.constant_name_go %> Algorithm = "<%= item.id %>"
<% }); %>
)

// IsValid checks if the algorithm is supported
func (a Algorithm) IsValid() bool {
	switch a {
	case <% taxonomies[0].items.forEach(function(item, index) { %><%= item.constant_name_go %><% if (index < taxonomies[0].items.length - 1) { %>, <% } %><% }); %>:
		return true
	}
	return false
}

// String returns the string representation
func (a Algorithm) String() string {
	return string(a)
}

<% data_structures.forEach(function(schema) { %>
// <%= schema.description %>
type <%= schema.className %> struct {
<% schema.properties.forEach(function(prop) { %>	// <%= prop.description %>
	<%= prop.name_go %> <%= prop.go_type %> `json:"<%= prop.name %><% if (!prop.is_required) { %>,omitempty<% } %>"`
<% }); %>}
<% }); %>
