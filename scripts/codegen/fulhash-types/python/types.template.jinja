"""
FulHash Types and Enums
Code generated by scripts/codegen/generate-fulhash-types.ts; DO NOT EDIT.
"""

from enum import StrEnum
from typing import TypedDict, Literal, List, Dict, Any, Optional

class Algorithm(StrEnum):
    """
    Supported hashing algorithms for FulHash
    """
    {% for item in taxonomies[0].items %}
    {{ item.constant_name }} = "{{ item.id }}"
    """{{ item.description }}"""
    {% endfor %}

{% for schema in data_structures %}
class {{ schema.className }}(TypedDict{% if schema.optional_properties %}, total=False{% endif %}):
    """
    {{ schema.description }}
    """
    {% for prop in schema.properties %}
    {{ prop.name }}: {{ prop.python_type }}
    """{{ prop.description }}"""
    {% endfor %}
{% endfor %}
