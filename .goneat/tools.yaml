version: v1.0.0
binDir: ./bin
tools:
  - id: curl
    description: HTTP client used for fetching remote assets
    required: true
    install:
      type: verify
      command: curl
  - id: rustup
    description: Rust toolchain installer
    required: false
    install:
      type: verify
      command: rustup
